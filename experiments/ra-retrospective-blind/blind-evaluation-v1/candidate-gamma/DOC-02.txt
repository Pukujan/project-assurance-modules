# Build versus reuse disposition

Candidate identities are only the names supplied in the frozen packet. Exact revisions, licenses, security posture, and API compatibility are unknown in this run and must be bound in M0 before implementation authorization.

| Capability | Serious supplied option(s) | Criteria | Cheap probe | Disposition | Rationale / reconsideration trigger |
| --- | --- | --- | --- | --- | --- |
| Immutable evidence, citation identity, event history, replay/rebuild | FOSSIL Core | exact identity; immutable history; reviewed promotion boundary; redaction/rebuild/replay; actor attribution; adapter surface | Contract fixtures for ingest, whole-artifact verification, review reference, failed ingest/orphan behavior, crash/replay, and rebuild | reuse | The packet describes direct substrate fit. If a scoped boundary fails, wrap or extend that seam only; do not replace the store first. |
| Dimension-separated promotion record and fail-closed policy | FOSSIL Core plus financial application; no supplied system is stated to bind all required dimensions to one promotion | typed results; exact evidence refs; deterministic checks; explicit review binding; fail-closed acceptance; replayable policy version | In-memory contract prototype with fixed fixtures and a FOSSIL seam | build_new | A small adapter/policy package earns bespoke work because the supplied systems are not stated to provide this contract. Stop or narrow it if FOSSIL's native reviewed-promotion boundary proves sufficient. |
| Finance workflow integration | Financial analysis application | user-flow fit; SEC temporal/version semantics; evidence-root binding; minimal invasive change | One representative promotion from source selection through reviewed result | wrap | The application retains domain/UX ownership. Reconsider if its integration surface cannot preserve exact evidence identity. |
| Source suitability / claim-source fit | Source Ranker | calibrated scope; explainable role; domain fit; contribution beyond deterministic policy and review; cost | Frozen finance development-set comparison with and without ranker signals | configure | Advisory/evaluation-only until a scoped measured benefit and owner-approved policy exist. |
| Retrieval evaluation | Retrieval Benchmarker | corpus/query/qrels identity; finance support; persisted rankings; composition with claim task; no runtime coupling | Frozen retrieval fixtures and sparse/dense/hybrid/rerank comparison | compose | Use reports as evidence for retrieval claims, not semantic correctness; retain outside runtime. |
| Claim/grounding dataset use | FEVER, SciFact, AVeriTeC, RAGTruth, ALCE; finance datasets | task/domain fit; label authority; licensing; leakage resistance; hidden holdout practicality | Dataset admission card and small frozen baseline | more_research | These are opportunities, not a universal metric. Admit only against a stated fallible claim. |
| Graph/vector projection | Existing retrieval/index tools if later justified | rebuildability, no-authority property, measured benefit | rebuild-equivalence and retrieval ablation | defer | No current slice claim needs it; admit after a frozen benchmark proves benefit over simpler baseline. |
| Agent/harness research-state layer | plugin-first, transport-neutral proposal | restart/idempotency; review binding; continuity benefit; friction | local microworld with forced restart after deterministic state tests | defer | A bounded experiment may return `NO_MEASURABLE_BENEFIT` and cannot block core work. |

No `build_new` item may enter implementation until the owner reviews this table, exact candidate revisions are recorded, and the named probe confirms unmet requirements or shows probing is disproportionate.
