# Candidates — not current work

Nothing here is an issue. Admission requires: scoped question, public-dev protocol, cost bound, kill/no-benefit rule, owner decision.

| ID | Candidate | Why it appeared in research | Why it is not v0 | Admission test |
|---|---|---|---|---|
| C1 | Claim verification product | FEVER/SciFact/AVeriTeC exist | Semantic support in v0 is human-bound; automatic claim eval is a different empirical problem | Does a checker reduce unsupported P-CLAIM accepts on public-dev without blocking exploration? |
| C2 | Grounding/citation LLM eval | RAGTruth/ALCE | Citation targeting v0 is deterministic identity/span match | Incremental gain over E3-1 on unsupported citations |
| C3 | GraphRAG / graph retrieval | Discussed as future mechanism | Indexes are projections; no measured need | Retrieval-layer ablation vs current retriever on public-dev finance; must beat cost |
| C4 | Ontology / meta-system | Discussed | No repeated failure showing lack of ontology blocks promotion | Named interoperability pain with actual users |
| C5 | Cross-domain packs | Packet warns validity does not transfer | Finance loop not real yet | After v0 finance success, a second domain owner, new gold/policy |
| C6 | TREC/BEIR campaign | Retrieval generalization | First need is finance retrieval reporting via existing Benchmarker | Explicit generalization question |
| C7 | Full PRISMA/Cochrane/SACM/PROV/SLSA import | Precedents | Interop ideas, not runtime deps | Export mapping after schema stable |
| C8 | MCP / ChatGPT adapters | Plugin research | Experiment may return no benefit; adapters after local tests | E8-1 green + measured handoff gain |
| C9 | Source Ranker as gate | Existing research repo | Broader than proven implementation | E6 comparison vs simple baseline |
| C10 | Agent continuity plugin as core | Interest in HITL/cross-session | Negative prior on stamp layers; packet says optional | E8 with `NO_MEASURABLE_BENEFIT` allowed |
| C11 | Mechanical SEC generator as hidden-only suite | Packet lists mechanical cases | Public-dev subset is in v0; hidden stays out | Confirmatory process after public-dev saturation |
| C12 | Any new mechanism from a single red-team finding | Findings will occur | D12 | Disposition: fix checker / policy / wontfix residual |

Kill language to copy onto any admission: “If public-dev does not show the pre-declared gain, the mechanism is removed from the accept path and recorded as `NO_MEASURABLE_BENEFIT`. It does not remain as optional magic.”
