# PAM gaps

## Fresh-workspace bootstrap

The frozen materials require a pinned methodology revision but do not provide a reusable bootstrap protocol for a fresh agent in an empty workspace: exact repository acquisition, detached revision verification, and the point at which candidate routing may begin. This run required an explicit operator instruction to fetch only the frozen SHA. The initial empty-workspace stop was pre-run and not contamination.

No methodology change was made during this run.
