# Event root

This directory is reserved for append-only FOSSIL-compatible event records produced when the reviewed PAM decision pack is actually ingested/promoted.

The bootstrap repository preserves source and proposed claims here without pretending that Git storage alone is a completed FOSSIL ingest transaction.
